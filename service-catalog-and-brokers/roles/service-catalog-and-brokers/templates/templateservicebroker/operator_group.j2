apiVersion: operators.coreos.com/v1alpha2
kind: OperatorGroup
metadata:
  name: template-service-broker-group
  namespace: "{{ _template_broker_project }}"
spec:
  targetNamespaces:
  - "{{ _template_broker_project }}"
